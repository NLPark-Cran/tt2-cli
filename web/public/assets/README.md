# assets 素材目录

此目录存放官网图片素材。

当前文件：
- `hero.webp` —— 首页 Hero 背景视觉（AI 生成：边缘节点网络，已接入首页，25% 透明度衬底）
- `og-cover.svg` —— og:image 分享封面占位（可替换为 1200×630 的 og-cover.png，需同步 BaseLayout 默认值）
- `avatar-placeholder.svg` —— 控制台用户头像兜底

预留位：
- `feature-*.svg` —— 特性图标（当前为组件内联 SVG，可抽到此处）
